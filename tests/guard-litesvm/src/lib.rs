//! No library code here on purpose ??? this crate exists only to hold the
//! composability spike in tests/pull_via_subscriptions.rs. Cargo requires
//! at least one target per package, so this file is that target.
